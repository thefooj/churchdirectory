<% 
  @rows_per_page = 6
  @cols_per_page = 3
%>

<style>

.maininfo .contact {
	display: none;
}
</style>
	<%= render :partial => 'title_page' %>

	<%= render :partial => 'member_pages_and_matrices', :locals => { :title => "#{@church.name} Members as of #{Date.today.strftime('%B %-d, %Y')}", :memberlist => @church.members, :rows_per_page => @rows_per_page, :cols_per_page => @cols_per_page, :show_details => false } %>


