<% 
  @rows_per_page = 6
  @cols_per_page = 3
%>

<style>

</style>
	<%= render :partial => 'title_page' %>
	
	<!-- members list -->
	<%= render :partial => 'member_pages_and_matrices', :locals => { :title => "#{@church.short_name_prioritized} Members as of #{Date.today.strftime('%B %-d, %Y')}", :memberlist => @church.attending_members, :rows_per_page => @rows_per_page, :cols_per_page => @cols_per_page, :show_details => false, :out_of_area => false } %>

	<!-- moved members are marked as  -->
	<%= render :partial => 'member_pages_and_matrices', :locals => { :title => "#{@church.short_name_prioritized} Members Out of Area as of #{Date.today.strftime('%B %-d, %Y')}", :memberlist => @church.moved_members, :rows_per_page => @rows_per_page, :cols_per_page => @cols_per_page, :show_details => false, :out_of_area => true } %>